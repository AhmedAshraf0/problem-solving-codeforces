//
// Created by CST on 08-Jan-22.
//
#include<iostream>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i = 0 ; i < n ; ++i)
        cin>>arr[i];
    if(n == 1)
    {
        cout<<"YES";
        return 0;
    }
    bool unique = false;
    for(int i = 0 ; i < n - 1 ; ++i)
    {
        if(arr[i] == arr[])
    }
}
